# Instructions
- For all tasks, use subagents whenever possible to optimize use of context and minimize unnecessary context in the main (orchestrator) conversation. Use subagents in parallel whenever possible as well to increase efficiency of completing tasks.
- Use the superpowers skill for all tasks.
- Test all newly-implemented features using, in order of preference: dev-browser (may need to reset relay and/or check for conflicting relays that are active on the same port) and Playwright MCP.
- If I propose something you don't think is ideal, question me about it and provide your rationale.
- If applicable: When explaining what you did for an output, don't just assume I know what variable names mean. Provide a short description as well if you think there's any chance I may not understand it at first look
- When creating or explaining forecast/model outputs, define jargon and ambiguous variables in plain business or clinical language. Avoid labels that could be mistaken for market share or pricing by disease burden, and explain segment labels such as source-of-business versus product pricing.
- In public-facing model diagrams, Sankeys, funnels, and patient-pool visuals, include nearby plain-language definitions for specialized segment terms or classes that readers may not know, such as durable option, source pool, eligible/access, and treated eyes.
- In public-facing forecast/model artifacts, do not show local workbook names, local file paths, internal sheet/cell references, or implementation notes that are meaningful only to the creator; keep traceability focused on source citations, assumptions, formulas, and reader-verifiable context.
- In public-facing forecast/model artifacts, avoid or remove meta interpretation-rule notes, creator-facing intent statements, or similar explanatory notes about what the artifact is meant to do; make the artifact self-explanatory through assumptions, formulas, source citations, and reader-verifiable context instead.
- In aggregate public-facing model-set copy, use inclusive set language such as "six pipeline programs" instead of singling out one program as an exception, unless the exception is analytically necessary and clearly defined.
- In public-facing forecast/model prose and visual text, every sentence of a question must start with a capital letter, even when the sentence follows a colon.
- In public-facing forecast/model visuals, avoid nonstandard or ambiguous clinical/business labels for patient pools, such as "opened"; use clearer wording such as eligible, considered, accessible, selected, or treated, depending on the actual model step.
- When a KPI, chart, or chart note compares a model output with an external benchmark or projection, labels and explanations must clearly distinguish the model's forecast value from the external benchmark value so readers do not mistake one for the other. If the external benchmark or projection value is known, include it alongside the model value so the comparison is self-contained.
- In public-facing forecast/model charts, avoid unnecessary qualifier words in chart labels, such as "explicit", unless the qualifier is needed to distinguish calculation logic or a benchmark; prefer plain reader-facing labels while preserving formula detail elsewhere.
- In public-facing forecast/model charts, when comparing forecast values across a small set of named drugs or products, prefer drug/product names as categorical x-axis labels and forecast values on the y-axis when that makes association clearer; plot each estimate on the corresponding category guide line.
- In public-facing multi-series charts, when one series marks selected data-point years, peer series should use the same marker cadence unless there is a clear reason not to, so readers can compare the same time points. When the user asks for labels on marked points, apply labels consistently across all marked points and series, using compact text and positioning to avoid collisions.
- In chart and section explanatory text, avoid obvious negative contrasts or redundant statements that only restate what the adjacent chart, table, or visual already shows; use the space to add interpretation, assumptions, or calculation context.
- In public-facing forecast/model visuals, every labeled variable or letter in a calculation key or diagram must include enough formula detail or derivation context for the reader to understand how its value is calculated, not just a definition.
- In public-facing patient-pool, funnel, or eligibility calculations, variables for downstream segments must explicitly show how they derive from the upstream total patient pool, including any intermediate filters or percentages, rather than only defining the segment name.
- In public-facing patient-pool or forecast-balance visuals, clearly distinguish starting balances from rolled-forward, year-specific balances, and define ramp multipliers in plain language as timing or adoption factors distinct from peak assumptions such as peak access share.
- In public-facing forecast/model visuals, when a displayed calculation uses intermediate quantities, those intermediate values must have visible labels, letters, or explicit cross-references in the calculation text so the reader can trace each number without relying on prose alone.
- Subagents must always be the same model and thinking level as the orchestrator agent.
- For evidence screenshots, close cookie banners, consent dialogs, modals, popups, and newsletter or marketing overlays before capture. If the page content is dimmed or a dark overlay covers the viewport, the screenshot is invalid; close or remove the overlay and retake it.
- When source text was accessible but the live page later blocks screenshot capture, create a clearly labeled saved-text evidence panel with highlighted relevant text rather than using a screenshot of the blocked/sign-in/security page.
- After updating evidence screenshots, spawn a fresh subagent to independently verify that the relevant text is highlighted and that no screenshot contains an obscuring popup, modal, consent banner, or dark overlay.
- Whenever the user provides feedback regarding an output, determine if it can be generalized to other similar situations. If so, then use a new subagent to deterministically update this file (AGENTS.md) accordingly. You can add new sections or edit existing sections in this file. Afterwards, check to make sure that the updates are localized only to the specific part of this file, as you should not be rewriting the entire file.
- In repeated public-facing visual card components, align action button rows, such as Infographic/Calculator controls, to a consistent distance from the bottom edge across cards.
- In repeated public-facing visual components, include padding on all sides so text, controls, and labels do not touch component borders or edges.
- In public-facing company/drug Infographic and Calculator pages, use color ranges that reflect each company's specific visual theme rather than a default shared palette.
- In public-facing visual outputs, inline letter-circle or badge markers that precede text must be vertically centered with the adjacent text.
- In public-facing visual outputs, letter-circle or badge markers that precede multiline calculation-key sections, callouts, or explanatory blocks must be top-aligned with the first line of adjacent text so the start of each section is clear.
- In public-facing visual outputs, chart legends must use clearly separated marker/label pairs with enough spacing or wrapping so entries do not run together.
- In public-facing visual outputs, size and wrap headings so they fit within their containers in no more than two lines, with no overflow or collision with adjacent content.
- In public-facing visual outputs, set line-height and vertical spacing for large wrapped headings so ascenders and descenders on adjacent lines do not touch.
- In public-facing visual outputs, keep numeric values and adjacent labels clearly separated from preceding letter-circle or badge markers, and ensure node titles or other text labels fit within their containers without overflow.
